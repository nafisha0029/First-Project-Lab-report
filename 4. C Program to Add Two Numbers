#include <stdio.h>

int main() {
    double a, b;
    printf("Enter two numbers: ");
    if (scanf("%lf %lf", &a, &b) == 2)
        printf("Sum = %.2f\n", a + b);
    else
        printf("Invalid input.\n");
    return 0;
}
