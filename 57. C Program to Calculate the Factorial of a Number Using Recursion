#include <stdio.h>
 unsigned long long fact(unsigned n) {
    if (n <= 1) return 1;
    return n * fact(n - 1);
 }
 int main() {
    unsigned n;
    printf("Enter n: ");
    if (scanf("%u", &n) != 1) return 0;
    printf("Factorial = %llu\n", fact(n));
    return 0;
 }
