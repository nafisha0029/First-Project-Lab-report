#include <stdio.h>
#include <string.h>
int main(){
    char s[]="level";
    char *l=s, *r=s+strlen(s)-1;
    while(l<r){ if(*l!=*r){ printf("Not palindrome\n"); return 0;} l++; r--; }
    printf("Palindrome\n"); return 0;
}
