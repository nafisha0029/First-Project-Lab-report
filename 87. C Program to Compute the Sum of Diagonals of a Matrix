#include <stdio.h>
int main(){
    int n=3,i;
    int a[3][3]={{1,2,3},{4,5,6},{7,8,9}};
    int sumMain=0,sumSec=0;
    for(i=0;i<n;i++){ sumMain+=a[i][i]; sumSec+=a[i][n-1-i]; }
    printf("Main=%d Secondary=%d Total=%d\n",sumMain,sumSec,sumMain+sumSec);
    return 0;
}
