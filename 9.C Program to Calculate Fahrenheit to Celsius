#include <stdio.h>

int main() {
    double f;
    printf("Enter temperature in Fahrenheit: ");
    if (scanf("%lf", &f) == 1) {
        double c = (f - 32.0) * 5.0 / 9.0;
        printf("%.2f F = %.2f C\n", f, c);
    } else {
        printf("Invalid input.\n");
    }
    return 0;
}
