#include <stdio.h>

int main() {
    FILE *in = fopen("input.txt", "r");
    FILE *out = fopen("output.txt", "w");

    char ch;
    while((ch = fgetc(in)) != EOF)
        fputc(ch, out);

    fclose(in);
    fclose(out);

    return 0;
}
