#include <stdio.h>
int main() {
    int a[]={10,20,30,40,50}, key=30;
    int l=0,r=4,mid;
    while(l<=r){
        mid=(l+r)/2;
        if(a[mid]==key){ printf("Found"); return 0; }
        else if(key<a[mid]) r=mid-1;
        else l=mid+1;
    }
    printf("Not Found");
}
