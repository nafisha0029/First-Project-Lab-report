#include <stdio.h>
 int main() {
    int n, i, j, count = 1;
    printf("Enter number of rows: ");
    if (scanf("%d", &n) != 1) return 0;
    for (i = 1; i <= n; i++) {
        for (j = 1; j <= i; j++) {
            printf("%d ", count++);
        }
        putchar('\n');
    }
    return 0;
 }
