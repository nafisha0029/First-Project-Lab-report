#include <stdio.h>

int stringLength(char *s) {
    int count = 0;

    while (s[count] != '\0') {
        count++;
    }
    return count;
}

int main() {
    char str[100] = "length";

    int len = stringLength(str);

    printf("Length of string: %d\n", len);

    return 0;
}
