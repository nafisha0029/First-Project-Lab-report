#include <stdio.h>
#include <ctype.h>
int main(){
    char s[]="hello world from c";
    int i=0;
    if(s[0] && !isspace(s[0])) printf("%c ", s[0]);
    for(i=1;s[i];i++) if(!isspace(s[i]) && isspace(s[i-1])) printf("%c ", s[i]);
    printf("\n"); return 0;
}
