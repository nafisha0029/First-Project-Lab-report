 #include <stdio.h>
 int main() {
    int n, i, j;
    printf("Enter number of rows: ");
    if (scanf("%d", &n) != 1) return 0;
    for (i = 1; i <= n; i++) {
        for (j = 0; j < i; j++) putchar('*');
        putchar('\n');
    }
    return 0;
 }
