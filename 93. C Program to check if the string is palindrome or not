#include <stdio.h>
#include <string.h>
int main(){
    char s[]="radar"; int i, n=strlen(s), pal=1;
    for(i=0;i<n/2;i++) if(s[i]!=s[n-1-i]) { pal=0; break; }
    printf("%s is %s\n", s, pal? "palindrome":"not palindrome");
    return 0;
}
